# Pending requests

Anyone can send a request from the app — they land here and do nothing yet.
The owner approves them with the code, which moves them into TASKS.md where
the auto-update agent picks them up.

